<?php
// Qual conteúdo mostrar dentro da área central da home.
// Vem da URL: ?page=home&view=cad-model
$view = $_GET['view'] ?? null;

// Lista branca de views permitidas — nunca dar require direto em
// algo vindo do usuário sem validar, senão vira Local File Inclusion.
$viewsPermitidas = [
    'cad-model'   => __DIR__ . '/../cad-model/cad-model.php',
    'cad-vei' => __DIR__ . '/../cad-veiculo/cad-vei.php',
    'cad-user'    => __DIR__ . '/../cad-user/cad-user.php',
];
?>
<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css">
<link rel="icon" type="image" href="https://upload.wikimedia.org/wikipedia/commons/9/96/Clube_de_Regatas_do_Flamengo_logo.svg">

<body>

<nav class="navbar navbar-expand-lg border-bottom">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold " style="color: #710707;" href="index.php?page=home">
            Car-Check
        </a>
    </div>
</nav>

<div class="d-flex">

    <div class="sidebar border-end">

        <a class="d-flex align-items-center text-white text-decoration-none">
            <span class="fs-4">Menu</span>
        </a>

        <hr>

        <ul class="nav nav-pills flex-column">

            <li class="nav-item">
                <a href="?page=home&view=cad-model"
                   class="nav-lin <?= $view === 'cad-model' ? 'active' : '' ?>"
                   style="color: white;">
                    Modelos
                </a>
            </li>

            <li class="nav-item">
                <a href="?page=home&view=cad-vei"
                   class="nav-lin <?= $view === 'cad-vei' ? 'active' : '' ?> text-white">
                    Veículos
                </a>
            </li>

            <li class="nav-item">
                <a href="?page=home&view=cad-user"
                   class="nav-lin <?= $view === 'cad-user' ? 'active' : '' ?> text-white">
                    Usuarios
                </a>
            </li>

        </ul>
        <hr style="margin-top: 44vh;">
        <div class="">
            <a href="/Sistema/pages/login/login.php" class="d-flex align-items-center text-white text-decoration-none">
                <svg width="32" height="32" class="rounded-circle me-2" xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-person-fill" viewBox="0 0 16 16">
                    <path d="M3 14s-1 0-1-1 1-4 6-4 6 3 6 4-1 1-1 1zm5-6a3 3 0 1 0 0-6 3 3 0 0 0 0 6"/>
                </svg>
                <strong>Minha conta</strong>
            </a>
        </div>

    </div>

    <div class="principal">
        <?php
        if ($view === null) {
            echo '<h2>Bem-vindo(a)!</h2><p>Selecione uma opção no menu ao lado.</p>';
        } elseif (isset($viewsPermitidas[$view])) {
            require $viewsPermitidas[$view];
        } else {
            echo '<p>Página não encontrada.</p>';
        }
        ?>
    </div>

</div>

</body>


*{
            margin:0;
            padding:0;
            box-sizing:border-box;
        }

body{
    min-height: 90vh;
    overflow: hidden;
}

        .bg-body-tertiary{
            background-color: rgb(52,69,87)!important;
        }

        .navbar-nav .nav-link{
            color:white;
        }

/* sidebar.css */
.sidebar {
  width: 220px;
  height: 100vh;
  background: #000000;
  border-right: 1px solid #3a3a3a;
  display: flex;
  flex-direction: column;
  flex-shrink: 0;
  overflow: hidden;
}

/* ── Logo ── */
#titulo {
  padding: 18px 14px;
  border-bottom: 1px solid #1f1e1e;
  display: flex;
  align-items: center;
  gap: 10px;
}

.hero-logo {
  width: 34px;
  height: 34px;
  background: #3C3489;
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 18px;
  flex-shrink: 0;
}

#titulo h3 {
  font-size: 13px;
  font-weight: 600;
  color: #ffffff;
  line-height: 1.2;
}



/* ── Navegação ── */
.principal {
  flex: 1;
  overflow-y: auto;
  display: flex;
  flex-direction: column;
  gap: 4px;
  width: 100vh;
}

.principal ul {
  list-style: none;
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.nav-label {
  font-size: 9px;
  color: #ffffff;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  font-weight: 600;
  padding: 12px 10px 4px;
}

.nav-iten {
  display: flex;
  align-items: center;
  gap: 9px;
  padding: 8px 10px;
  border-radius: 8px;
  font-size: 13px;
  color: #A09EBB;
  cursor: pointer;
  transition: background 0.15s, color 0.15s;
}

.nav-iten:hover {
  background: #121957;
  color: #ffffff;
}

.nav-iten.active {
  background: #121957;
  color: #ffffff;
  font-weight: 500;
}

/* ── Rodapé ── */
.baixo {
  padding: 12px 14px;
  border-top: 1px solid #3a3a3a;
  display: flex;
  align-items: center;
  gap: 9px;
  top: 178%;
}

.hero-visual img{
  width: 32%;
  height: 32%;
  border-radius: 50%;
  background: #3C3489;
  color: #ffffff;
  display: flex;
  align-items: center;
  justify-content: center;
  font-size: 11px;
  font-weight: 600;
  flex-shrink: 0;
}

/* ── Conta (dropdown) — adicionar ao final de sidebar.css ── */
.baixo {
  position: relative;
}

.conta-btn {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 9px;
  background: none;
  border: none;
  color: #A09EBB;
  font-size: 12px;
  font-family: inherit;
  cursor: pointer;
  padding: 6px 4px;
  border-radius: 8px;
  transition: background 0.15s, color 0.15s;
}
.conta-btn:hover {
  background: #121957;
  color: #ffffff;
}
.conta-btn svg { font-size: 16px; flex-shrink: 0; }

.conta-menu {
  position: absolute;
  bottom: calc(100% + 8px);
  left: 14px;
  right: 14px;
  background: #121212;
  border: 1px solid #2a2a2a;
  border-radius: 10px;
  overflow: hidden;
  box-shadow: 0 8px 24px rgba(0, 0, 0, 0.4);
  animation: contaMenuFade 0.12s ease;
  z-index: 20;
}

@keyframes contaMenuFade {
  from { opacity: 0; transform: translateY(4px); }
  to   { opacity: 1; transform: translateY(0); }
}

.conta-menu-item {
  width: 100%;
  display: flex;
  align-items: center;
  gap: 9px;
  background: none;
  border: none;
  color: #d8d6ef;
  font-size: 12.5px;
  font-family: inherit;
  padding: 10px 12px;
  cursor: pointer;
  transition: background 0.12s;
  text-align: left;
}
.conta-menu-item svg { font-size: 14px; flex-shrink: 0; }
.conta-menu-item:hover { background: #1c1c2e; }

.conta-menu-sair {
  color: #E24B4A;
  border-top: 1px solid #232323;
}
.conta-menu-sair:hover { background: #2a1414; }

.p{
    color: #fff;
}

.nav-lin {
    display: block;
    position: relative;
    text-decoration: none;
    background: 0 0;
    border: 0;
    transition: color .15s ease-in-out, background-color .15s ease-in-out, border-color .15s ease-in-out;
    padding: 5px;
        border-radius: 5px
}